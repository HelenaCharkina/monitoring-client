export default class Agent {
    public id: string
    public name: string
    public ip: string
    public port: string
    public schedule: string
    public description: string
    public state: number
}